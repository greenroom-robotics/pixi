package:
  name: pkg-b
  version: 0.1.0

build:
  script:
    - if: win
      then:
        - if not exist "%PREFIX%" mkdir "%PREFIX%"
        - echo hello from pkg-b! > "%PREFIX%\hi"
      else:
        - mkdir -p $PREFIX
        - echo "hello from pkg-b!" > $PREFIX/hi
